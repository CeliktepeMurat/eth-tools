import React from 'react';

import EncodePage from './app/encode/EncodePage';

interface Props {}

const App = (props: Props) => {
  return (
    <div>
      <EncodePage />
    </div>
  );
};

export default App;
